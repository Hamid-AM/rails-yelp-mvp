<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: ["1", "2", "3", "4", "5"],  prompt: "rating" %>
  <%= f.input :content %>
  <%= f.submit "Add review" %>
<% end %>
